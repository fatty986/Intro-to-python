# Post Exercise – Sum of the First n Integers

You are given a positive integer `n`.

Your task is to compute the sum:

1 + 2 + 3 + ... + n

---

## Examples

- If `n = 1`, the result is `1`
- If `n = 3`, the result is `6`
- If `n = 5`, the result is `15`

---

## Output

Print the result.

Do not use `input()`.
