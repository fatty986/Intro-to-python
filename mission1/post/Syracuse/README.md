# Post Exercise – Syracuse Sequence

You are given a positive number `n`.

If `n` is even:
- divide it by 2

If `n` is odd:
- multiply it by 3 and add 1

Repeat these steps until the value becomes `1`.

---

## What to do

- Print the value of `n`
- Change `n` using the rules
- Stop when `n` becomes `1`
- Print `1` at the end

Each number must be printed on a new line.

---

## Rules

- Do NOT use input()
- Use a loop
- Use print()
